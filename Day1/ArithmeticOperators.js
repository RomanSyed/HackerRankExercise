function getArea(length, breadth) {
  let area = length * breadth;
  return area;
}
function getPerimeter(length, breadth) {
  let perimeter = 2 * (length + breadth);
  return perimeter;
}
console.log(getArea(2, 5));
console.log(getPerimeter(3, 8));
